{
  "name": "tls-monitor",
  "version": "1.0.0",
  "description": "TLS/SSL Certificate Monitor — Netlify",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step needed'"
  },
  "dependencies": {
    "node-forge": "^1.3.1"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
